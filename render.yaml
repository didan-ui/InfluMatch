services:
  - type: web
    name: influmatch-app
    runtime: node
    buildCommand: npm install && vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production